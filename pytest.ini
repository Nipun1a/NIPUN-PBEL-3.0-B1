[pytest]
# Custom marks used in this project
markers =
    integration: slow end-to-end tests that load the real face recognition model
                 (skip with: pytest -m "not integration")

# Show short tracebacks by default; use -v for full output
addopts = --tb=short

# asyncio mode for pytest-asyncio
asyncio_mode = auto
